import renderer from 'react-test-renderer';
import React from 'react';
// import { EntryDetailContainer } from '../EntryDetailContainer';

// mock wrapped connected component
// jest.mock('../EntryContentToolbarContainer', () => 'EntryContentToolbarContainer');

describe('EntryDetailContainer', () => {
  it('renders correctly', () => {
//     const tree = renderer.create(
//       <EntryDetailContainer
//         entries={[]}
//         entry={{}}
//         params={{}}
//         pathname="path"
//         decrementUnreadCount={() => {}}
//         requestMarkEntryAsRead={() => {}}
//         push={() => {}}
//       />
//     ).toJSON();

//     expect(tree).toMatchSnapshot();
  });
});
