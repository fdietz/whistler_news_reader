language: elixir
elixir:
  - 1.2.6
otp_release:
  - 18.3
addons:
  postgresql: '9.4'
services:
  - postgresql
before_script:
  - mix do ecto.create, ecto.migrate
script:
  - mix test --exclude integration
